{
  "name": "ledisa-quiz-funnel",
  "version": "0.0.0",
  "private": true,
  "description": "Performance-focused, plain HTML/CSS/JS quiz funnel for Ledisa.",
  "scripts": {
    "build:config": "node scripts/build-config.js",
    "dev": "npm run build:config && npx serve ."
  }
}
